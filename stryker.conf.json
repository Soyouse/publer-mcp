{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "_comment": "Gate mutation : cliquet anti-régression. break = plancher dur (CI/push échoue en dessous). Monter break au niveau de high quand le score grimpe — JAMAIS le redescendre.",
  "packageManager": "npm",
  "_comment_incremental": "Mode incrémental (diff-based) : ne re-mute QUE le code changé via reports/stryker-incremental.json, rapport COMPLET conservé. Fichier absent/corrompu → fallback run complet (sûr).",
  "incremental": true,
  "testRunner": "vitest",
  "reporters": [
    "html",
    "clear-text",
    "progress"
  ],
  "coverageAnalysis": "perTest",
  "_comment_io": "Le fetch réseau de client.js (doFetch) est I/O — prouvé live, pas par mutation. La logique PURE (normalizeSecrets, resolveWorkspaceId, buildRequest, méthode) est mutée. server.js/http.js = transports I/O, exclus.",
  "mutate": [
    "lib/core/client.js",
    "lib/http-auth.js",
    "lib/rate-monitor.js",
    "lib/registry.js",
    "handlers/*.js",
    "dispatch.js",
    "incidents.js",
    "lib/deps-criticite-pure.js"
  ],
  "thresholds": {
    "high": 85,
    "low": 80,
    "break": 80
  },
  "concurrency": 3,
  "timeoutMS": 60000,
  "_config_minimale": "OBLIGATOIRE (CLAUDE.md, local ET CI) : incremental:true + coverageAnalysis:perTest + timeoutMS explicite + concurrency bornee 2-3. Propage depuis pw-mcp-proxy le 03/08/2026. ⚠️ concurrency NON BORNEE sature la machine (Stryker prend tous les coeurs) — les garde-fous sautent a 100% CPU. ⚠️ timeoutMS absent = un mutant qui EXPIRE est compte comme TUE => score GONFLE et FAUX."
}
